Prefer local files, shell, Python, and free public APIs; once they produce a sufficient answer, stop—do not search for a paid replacement.
Buy only when current structured data, rendering, hosting, or anti-bot access saves turns. Inspect the exact live schema and price before paying; validate output and receipt.

- Local data, arithmetic, validation, transforms -> `python3`/`jq`/local files -> read once, compute, save requested artifact -> $0 -> strongest default -> never buy parsing, JSON repair, translation, token counting, checksum, or ICS work that is locally feasible.
- Known public page -> `curl -L`/Python HTTP -> GET the supplied URL and parse source locally -> $0 -> strongest for static documents and metadata -> do not invoke paid discovery after success.
- Web search -> `https://stableenrich.dev/api/exa/search` -> inspect, POST query plus result controls -> typically $0.007–$0.02 -> 4.23/5, n_eff 12.4 -> use direct URLs/local corpus first.
- Page extraction -> `https://stableenrich.dev/api/exa/contents` -> inspect, POST target URL and content options -> price unreported -> 4.23/5, n_eff 4.3 -> use curl/parser first; use a browser for JS-heavy pages.
- Image generation -> `https://stablestudio.dev/api/generate/flux-2-pro/generate` -> inspect dynamic schema, POST prompt, poll returned URL without repeated discovery, download artifact -> $0.40–$3.20 observed -> 3.94/5, n_eff 6.6 -> use existing/local assets when generation is unnecessary.
- Public upload -> `https://stableupload.dev/api/upload` -> POST filename, contentType, tier; upload through returned signed URL and verify public URL -> $0.005 short slot; up to ~$2 observed -> 4.04/5, n_eff 9.7 -> keep local unless public retrieval is required.
- Flights -> `https://stabletravel.dev/api/google-flights/search` -> GET departure_id, arrival_id, dates, type, filters -> $0.02 -> 4.25/5, n_eff 3.4 -> use airline/browser search when sufficient.
- Current weather -> `https://agenttoll.dev/paid/env/weather-current` -> inspect, POST coordinates, units, variables -> price unreported -> 3.91/5, n_eff 1.7 -> use free weather feeds first and stop if they answer.
- Crypto market report -> `https://bazaar-gateway.vercel.app/api/market-report` -> inspect coin query; parse structured fields and markdown -> $0.001 -> 4.25/5, n_eff 3.3 -> use free exchange quotes for simple prices.
- Drug products -> `https://api.rxatlas.dev/v1/drugs` -> inspect, query ingredient/source/limit/count and follow cursor -> $0.10 search -> 3.95/5, n_eff 4.9 -> use RxNorm/openFDA for simple identifiers or public records.
